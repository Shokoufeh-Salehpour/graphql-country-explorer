import { CountryDetails } from '../components/CountryDetails';

export function CountryPage() {
  return (
    <section className="explorer" aria-label="Country details">
      <CountryDetails />
    </section>
  );
}
